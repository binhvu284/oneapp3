// Re-export from context for convenience
export { useAuthSource, AuthSourceProvider } from "@/contexts/AuthSourceContext";
export type { PortableUser, AuthMode, AuthResult, CachedSystemConnection } from "@/contexts/AuthSourceContext";
